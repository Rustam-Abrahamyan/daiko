import { Box, Title } from '@mantine/core';

export default function AuthPage() {
    return (
        <Box px="md" py="xs">
            <Title order={2} fw="400" mb="md">
                Auth
            </Title>
        </Box>
    );
}
